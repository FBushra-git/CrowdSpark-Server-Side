# CrowdSpark Server

TypeScript Express API for the CrowdSpark crowdfunding platform.

## Scripts

- `npm run dev` starts the API in watch mode.
- `npm run build` compiles TypeScript to `dist`.
- `npm start` runs the compiled server.

Copy `.env.example` to `.env` and fill in MongoDB, JWT, imgBB, and Stripe values.
